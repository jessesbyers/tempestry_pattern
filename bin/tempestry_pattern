#!/usr/bin/env ruby

require './lib/tempestry_pattern'
require 'sqlite3'

DB = {:conn => SQLite3::Database.new('./db/colors.db')}

# if TempestryPattern::Color.colors.empty?
TempestryPattern::Color.all
# end

TempestryPattern::CLI.new.call
